<%= form_for(@anon_report) do |f| %>
  <% if @anon_report.errors.any? %>
    <div id="error_explanation" style="width: 100%">
      <h2><%= pluralize(@anon_report.errors.count, "error") %> prohibited this anon_report from being saved:</h2>

      <ul>
      <% @anon_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :short_desc %><br>
    <%= f.text_area :short_desc %>
  </div>

  <div class="field">
    <%= f.label :full_report %><br>
    <%= f.text_area :full_report %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
