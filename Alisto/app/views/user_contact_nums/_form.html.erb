<%= form_for(@user_contact_num) do |f| %>
  <% if @user_contact_num.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_contact_num.errors.count, "error") %> prohibited this user_contact_num from being saved:</h2>

      <ul>
      <% @user_contact_num.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :num %><br>
    <%= f.text_field :num %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
